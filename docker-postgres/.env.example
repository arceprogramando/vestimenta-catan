# PostgreSQL Configuration for Docker
POSTGRES_USER=appuser
POSTGRES_PASSWORD=supersegura123
POSTGRES_DB=vestimenta_catan_db

# Network
POSTGRES_PORT=5432              # interno en el contenedor
POSTGRES_EXTERNAL_PORT=5433     # expuesto en el host

# Timezone
TZ=America/Argentina/Buenos_Aires

# Database URL para conexión desde aplicaciones
DATABASE_URL=postgresql://appuser:supersegura123@localhost:5432/vestimenta_catan_db?schema=public

# Puerto externo (mapeo del contenedor)
POSTGRES_EXTERNAL_PORT=5432
