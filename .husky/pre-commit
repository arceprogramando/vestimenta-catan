# Pre-commit hook: Corre tests y lint antes de cada commit
# Si fallan, el commit se cancela

echo "🧪 Running tests..."
cd vestimenta-catan-api && pnpm run test

echo "🔍 Running lint..."
cd vestimenta-catan-api && pnpm run lint

echo "✅ All checks passed!"
